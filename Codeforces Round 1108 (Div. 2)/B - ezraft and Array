#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl = '\n';
using vi = vector<int>;
using vll = vector<long long>;
using vs = vector<string>;
using si = set<int>;
using sll = set<long long>;
using st = stack<int>;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {

        ll n;
        cin >> n;

        if(n==1)
        {
            cout<<1<<nl;
        }
        else if(n==2)
        {
            cout<<-1<<nl;
        }
        else
        {
            vll a;
            a.push_back(1);
            a.push_back(2);
            a.push_back(3);

            ll x=3;
            for(int i=4;i<=n;i++)
            {
                x=x*2;
                a.push_back(x);
            }

            for(int i=0;i<n;i++)
            {
                cout<<a[i]<<" ";


            }
            cout<<nl;


        }









    }

}
