#include<bits/stdc++.h>
using namespace std;
 
typedef long long ll;
const char nl='\n';
using vi = vector<int>;
 
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
 
        int sum=0;
        int n=7;
        vi a(n);
        for(int i=0;i<n;i++)
        {
            cin>>a[i];
            sum+=a[i];
        }
 
        int mx=*max_element(a.begin(), a.end());
 
        cout<<2*mx-sum<<nl;
 
 
 
 
 
    }
 
}
