#include<bits/stdc++.h>
using namespace std;
 
typedef long long int ll;
typedef unsigned long long int  llu;
 
int main()
{
   int t;
   cin>>t;
   while(t--)
   {
        int n;
        cin>>n;
 
        vector<int> v(n);
        bool found = false;
 
        for(int i=0;i<n;i++){
            cin>> v[i];
            if(v[i] == 67){
                found=true;
            }
        }
 
        if(found)
            cout<<"YES"<<endl;
        else
            cout<<"NO"<<endl;
 
   }
 
}
