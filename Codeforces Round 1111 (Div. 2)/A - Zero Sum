#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl = '\n';
using vi = vector<int>;
using vll = vector<long long>;
using vs = vector<string>;
using si = set<int>;
using sll = set<long long>;
using st = stack<int>;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {

        int n;
        cin>>n;

        int sum=0;
        vi a(n);
        for(int i=0;i<n;i++)
        {
            cin>>a[i];
            sum+=a[i];

        }
        if(n%2==1)
        {
            cout<<"NO"<<nl;
        }
        else if(sum%4==0)
        {
            cout<<"YES"<<nl;
        }
        else
        {
            cout<<"NO"<<nl;
        }






    }

}
