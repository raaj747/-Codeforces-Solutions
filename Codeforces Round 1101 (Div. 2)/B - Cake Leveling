#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl = '\n';
using vi = vector<int>;
using vll = vector<long long>;
using vs = vector<string>;
using si = set<int>;
using sll = set<long long>;
using st = stack<int>;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;

        ll sum=0;
        ll ans=LLONG_MAX;
        for(int i=1;i<=n;i++)
        {
            ll x;
            cin>>x;

            sum+=x;
            ans=min(ans,sum/i);

            cout<<ans<<" ";





        }

        cout<<nl;






    }

}
