#include<bits/stdc++.h>
using namespace std;
 
typedef long long ll;
const char nl='\n';
using vi = vector<int>;
 
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
 
        int n,k;
        cin>>n>>k;
 
        int sum=0;
        for(int i=0;i<n;i++)
        {
            int x;
            cin>>x;
            sum+=x;
        }
 
        int mx=n*k;
 
        if(sum%2==1)
        {
            cout<<"YES"<<nl;
        }
        else
        {
            if(mx%2==0)
            {
                cout<<"YES"<<nl;
            }
            else
            {
                cout<<"NO"<<nl;
            }
        }
 
 
 
 
 
    }
 
}
