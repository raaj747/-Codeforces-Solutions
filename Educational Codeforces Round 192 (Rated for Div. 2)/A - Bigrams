#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl = '\n';
using vi = vector<int>;
using vll = vector<long long>;
using vs = vector<string>;
using si = set<int>;
using sll = set<long long>;
using st = stack<int>;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {

        ll n;
        cin>>n;

        int cnt=0;
        bool ok=false;
        for(int i=0;i<n;i++)
        {
            ll x;
            cin>>x;
            if(x>=3)
            {
                ok=true;
            }

            if(x>=2)
            {
                cnt++;
            }



        }

        if(ok||cnt>=2)
        {
            cout<<"YES"<<nl;
        }
        else
        {
            cout<<"NO"<<nl;
        }






    }

}
