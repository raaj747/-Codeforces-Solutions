#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl = '\n';
using vi = vector<int>;
using vll = vector<long long>;
using vs = vector<string>;
using si = set<int>;
using sll = set<long long>;
using st = stack<int>;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {

        int n;
        cin>>n;

        vi a,b,c,d;
        for(int i=0;i<n;i++)
        {


            int x;
            cin>>x;

            if(x%6==0)
            {
                c.push_back(x);
            }
            else if(x%2==0)
            {
                a.push_back(x);
            }
            else if(x%3==0)
            {
                b.push_back(x);
            }
            else
            {
                d.push_back(x);
            }


        }

        for(int x:c)
        {
            cout<<x<<" ";
        }
        for(int x:a)
        {
            cout<<x<<" ";
        }
        for(int x:d)
        {
            cout<<x<<" ";
        }
        for(int x:b)
        {
            cout<<x<<" ";
        }
        cout<<nl;






    }

}
