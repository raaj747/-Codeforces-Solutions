#include<bits/stdc++.h>
using namespace std;
 
typedef long long ll;
const char nl='\n';
using vi = vector<int>;
using vll = vector<long long>;
 
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin>>n;
        set<int>st;
        for(int i=0;i<n;i++)
        {
            int x;
            cin>>x;
            st.insert(x);
        }
        if(st.size()<n)
        {
            cout<<"-1"<<nl;
        }
        else
        {
            for(auto it=st.rbegin();it!=st.rend();it++)
            {
                cout<<(*it)<<" ";
            }
            cout<<nl;
        }
 
 
 
 
 
 
 
    }
 
}
